light_inf.starport:
	Inherits: light_inf
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~techlevel.1
		BuildPaletteOrder: 1
		Queue: Starport
	Valued:
		Cost: 60
	RenderSprites:
		Image: light_inf

trooper.starport:
	Inherits: trooper
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~techlevel.2
		BuildPaletteOrder: 2
		Queue: Starport
	Valued:
		Cost: 100
	RenderSprites:
		Image: trooper

dog.starport:
	Inherits: dog
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~barracks.smuggler, ~techlevel.5
		BuildPaletteOrder: 3
		Queue: Starport
	Valued:
		Cost: 90
	RenderSprites:
		Image: dog

engineer.starport:
	Inherits: engineer
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~techlevel.3
		BuildPaletteOrder: 4
		Queue: Starport
	Valued:
		Cost: 750
	RenderSprites:
		Image: engineer

thumper.starport:
	Inherits: thumper
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~techlevel.7
		BuildPaletteOrder: 5
		Queue: Starport
	Valued:
		Cost: 300
	RenderSprites:
		Image: thumper

grenadier.starport:
	Inherits: grenadier
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~barracks.atreides, ~techlevel.5
		BuildPaletteOrder: 6
		Queue: Starport
	Valued:
		Cost: 105
	RenderSprites:
		Image: grenadier

nsfremen.starport:
	Inherits: nsfremen
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~barracks.freman, ~techlevel.5
		BuildPaletteOrder: 7
		Queue: Starport
	Valued:
		Cost: 220

mechanic.starport:
	Inherits: mechanic
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~barracks.ixian, ~techlevel.5
		BuildPaletteOrder: 8
		Queue: Starport
	Valued:
		Cost: 310
	RenderSprites:
		Image: mechanic

flamethrower.starport:
	Inherits: flamethrower
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~barracks.harkonnen, ~techlevel.5
		BuildPaletteOrder: 9
		Queue: Starport
	Valued:
		Cost: 325
	RenderSprites:
		Image: flamethrower

plasma_infantry.starport:
	Inherits: plasma_infantry
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~barracks.corrino, ~techlevel.5
		BuildPaletteOrder: 10
		Queue: Starport
	Valued:
		Cost: 190
	RenderSprites:
		Image: plasma_infantry

sardaukar.starport:
	Inherits: sardaukar
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~barracks.corrino, ~techlevel.7
		BuildPaletteOrder: 11
		Queue: Starport
	Valued:
		Cost: 170
	RenderSprites:
		Image: sardaukar

harvester.starport:
	Inherits: harvester
	Buildable:
		Prerequisites: starport, ~!starport.smuggler, ~techlevel.1
		BuildPaletteOrder: 12
		Queue: Starport
	Valued:
		Cost: 1500
	RenderSprites:
		Image: harvester

harvester.smuggler.starport:
	Inherits: harvester.smuggler
	Buildable:
		Prerequisites: starport, ~starport.smuggler, ~techlevel.1
		BuildPaletteOrder: 13
		Queue: Starport
	Valued:
		Cost: 1250
	RenderSprites:
		Image: harvester

trike.mg.starport:
	Inherits: trike.mg
	Buildable:
		Prerequisites: starport, ~starport.mgtrike, ~techlevel.1
		BuildPaletteOrder: 14
		Queue: Starport
	Valued:
		Cost: 375
	RenderSprites:
		Image: trike

raider.mg.starport:
	Inherits: raider.mg
	Buildable:
		Prerequisites: starport, ~starport.mgraider, ~techlevel.1
		BuildPaletteOrder: 15
		Queue: Starport
	Valued:
		Cost: 375

quad.mg.starport:
	Inherits: quad.mg
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~light.mgquad, ~techlevel.1
		BuildPaletteOrder: 16
		Queue: Starport
	Valued:
		Cost: 375

trike.rocket.starport:
	Inherits: trike.rocket
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~light.rockettrike, ~techlevel.2
		BuildPaletteOrder: 17
		Queue: Starport
	Valued:
		Cost: 475
	RenderSprites:
		Image: trike

raider.rocket.starport:
	Inherits: raider.rocket
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~light.rocketraider, ~techlevel.2
		BuildPaletteOrder: 18
		Queue: Starport
	Valued:
		Cost: 475

quad.rocket.starport:
	Inherits: quad.rocket
	Buildable:
		Prerequisites: starport, ~techlevel.2
		BuildPaletteOrder: 19
		Queue: Starport
	Valued:
		Cost: 475

stealth_raider.starport:
	Inherits: stealth_raider
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~light.ordos, ~techlevel.5
		BuildPaletteOrder: 20
		Queue: Starport
	Valued:
		Cost: 550
	RenderSprites:
		Image: stealth_raider

light_tank.starport:
	Inherits: light_tank
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~techlevel.5
		BuildPaletteOrder: 21
		Queue: Starport
	Valued:
		Cost: 575
	RenderSprites:
		Image: light_tank

combat_tank_a.starport:
	Inherits: combat_tank_a
	Buildable:
		Prerequisites: ~starport.atreides, ~techlevel.3
		BuildPaletteOrder: 22
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: combat_tank_a

combat_tank_h.starport:
	Inherits: combat_tank_h
	Buildable:
		Prerequisites: ~starport.harkonnen, ~techlevel.3
		BuildPaletteOrder: 23
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: combat_tank_h

combat_tank_o.starport:
	Inherits: combat_tank_o
	Buildable:
		Prerequisites: ~starport.ordos, ~techlevel.3
		BuildPaletteOrder: 24
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: combat_tank_o

combat_tank_f.starport:
	Inherits: combat_tank_f
	Buildable:
		Prerequisites: ~starport.freman, ~techlevel.3
		BuildPaletteOrder: 25
		Queue: Starport
	Valued:
		Cost: 775
	RenderSprites:
		Image: combat_tank_f

combat_tank_m.starport:
	Inherits: combat_tank_m
	Buildable:
		Prerequisites: ~starport.mercenary, ~techlevel.3
		BuildPaletteOrder: 26
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: combat_tank_m

combat_tank_s.starport:
	Inherits: combat_tank_s
	Buildable:
		Prerequisites: ~starport.smuggler, ~techlevel.3
		BuildPaletteOrder: 27
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: combat_tank_s

combat_tank_c.starport:
	Inherits: combat_tank_c
	Buildable:
		Prerequisites: ~starport.corrino, ~techlevel.3
		BuildPaletteOrder: 28
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: combat_tank_c

combat_tank_i.starport:
	Inherits: combat_tank_i
	Buildable:
		Prerequisites: ~starport.ixian, ~techlevel.3
		BuildPaletteOrder: 29
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: combat_tank_i

siege_tank.starport:
	Inherits: siege_tank
	Buildable:
		Prerequisites: starport, ~techlevel.5
		BuildPaletteOrder: 30
		Queue: Starport
	Valued:
		Cost: 1075
	RenderSprites:
		Image: siege_tank

mcv.starport:
	Inherits: mcv
	Buildable:
		Prerequisites: starport, ~techlevel.3
		BuildPaletteOrder: 31
		Queue: Starport
	Valued:
		Cost: 2500
	RenderSprites:
		Image: mcv

missile_tank.starport:
	Inherits: missile_tank
	Buildable:
		Prerequisites: starport, ~techlevel.4
		BuildPaletteOrder: 32
		Queue: Starport
	Valued:
		Cost: 1250
	RenderSprites:
		Image: missile_tank

sonic_tank.starport:
	Inherits: sonic_tank
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.atreides, ~techlevel.6
		BuildPaletteOrder: 33
		Queue: Starport
	Valued:
		Cost: 1200
	RenderSprites:
		Image: sonic_tank

stealth_tank.starport:
	Inherits: stealth_tank
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.freman, ~techlevel.6
		BuildPaletteOrder: 34
		Queue: Starport
	Valued:
		Cost: 1075
	RenderSprites:
		Image: stealth_tank

jammer.starport:
	Inherits: jammer
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.ixian, ~techlevel.6
		BuildPaletteOrder: 35
		Queue: Starport
	Valued:
		Cost: 1200
	RenderSprites:
		Image: jammer

devastator.starport:
	Inherits: devastator
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.harkonnen, ~techlevel.6
		BuildPaletteOrder: 36
		Queue: Starport
	Valued:
		Cost: 1275
	RenderSprites:
		Image: devastator

quake_tank.starport:
	Inherits: quake_tank
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.corrino, ~techlevel.6
		BuildPaletteOrder: 37
		Queue: Starport
	Valued:
		Cost: 1750
	RenderSprites:
		Image: quake_tank

deviator.starport:
	Inherits: deviator
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.ordos, ~techlevel.6
		BuildPaletteOrder: 38
		Queue: Starport
	Valued:
		Cost: 1200
	RenderSprites:
		Image: deviator

bomb_buggy.starport:
	Inherits: bomb_buggy
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.mercenary, ~techlevel.6
		BuildPaletteOrder: 39
		Queue: Starport
	Valued:
		Cost: 875
	RenderSprites:
		Image: bomb_buggy

ifv.starport:
	Inherits: ifv
	Buildable:
		Prerequisites: starport, ~starport.mercenary, ~heavy.smuggler, ~techlevel.6
		BuildPaletteOrder: 40
		Queue: Starport
	Valued:
		Cost: 1050
	RenderSprites:
		Image: ifv

carryall.starport:
	Inherits: carryall
	Buildable:
		Prerequisites: starport, ~!starport.smuggler, ~techlevel.4
		BuildPaletteOrder: 41
		Queue: Starport
	Valued:
		Cost: 1500

carryall.smuggler.starport:
	Inherits: carryall.smuggler
	Buildable:
		Prerequisites: starport, ~starport.smuggler, ~techlevel.4
		BuildPaletteOrder: 42
		Queue: Starport
	Valued:
		Cost: 1500
